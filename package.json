{
  "name": "nglcuba",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p out && cp index.html out/ && cp demo.html out/demo.html",
    "start": "serve out",
    "lint": "echo 'No lint needed'"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
